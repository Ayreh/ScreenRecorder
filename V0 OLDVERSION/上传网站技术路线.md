# 开发路线图与存储团队配合文档

## 一、开发路线图（总周期预估 4-6 周，按阶段划分）

### 阶段 1：需求分析与技术选型（1 周）

#### 核心任务

1. 明确网页端核心功能边界（已确认：文件上传、断点续传、MD5 校验、日志查询、上传编码生成）；

2. 选定前端技术栈（推荐成熟组合，降低开发成本）：

   * 框架：Vue 3（Composition API）或 React 18（Hooks）（二选一，优先团队熟悉技术）；

   * 上传组件：基于 axios 封装分块上传逻辑，或使用成熟库（如 vue-upload-component/react-dropzone，支持断点续传）；

   * 加密与校验：前端 spark-md5 库计算文件 MD5（支持大文件分片计算，避免内存溢出）；

   * 接口交互：封装 S3/R2 上传 API 调用层（统一处理请求头、签名、异常）。

3. 输出《技术选型文档》《前端功能模块拆分表》。

#### 存储团队配合事项

| 配合内容          | 交付物                                                                 | 用途                                                                 |
|-------------------|------------------------------------------------------------------------|----------------------------------------------------------------------|
| 提供 S3/R2 基础信息 | 1. S3 测试桶名 + 悉尼区域（如 game-recording-test-apse2）<br>2. R2 测试桶名 + 边缘节点区域（如 game-recording-transit-test）<br>3. 桶内目录结构确认（[供应商代码]/[游戏名]） | 技术选型时确认 SDK 适配性（如 AWS SDK for JavaScript、Cloudflare R2 SDK），避免后期因存储参数变更调整代码 |

### 阶段 2：开发环境搭建（0.5 周）

#### 核心任务

1. 搭建前端项目骨架（含路由、状态管理、请求拦截器）：

   * 路由划分：/login（供应商登录）、/upload（文件上传页）、/log-query（日志查询页）、/report-download（校验报告下载页）；

   * 请求拦截器：统一添加认证头（如供应商登录 Token）、处理 S3/R2 返回的 4xx/5xx 异常（如权限不足、上传超时）；

2. 配置开发环境变量（区分测试 / 生产环境）：

   * 测试环境：S3/R2 测试桶地址、临时 AccessKey/SecretAccessKey；

   * 生产环境：预留占位符（后期替换为存储团队提供的生产密钥）。

#### 存储团队配合事项

| 配合内容          | 交付物                                                                 | 用途                                                                 |
|-------------------|------------------------------------------------------------------------|----------------------------------------------------------------------|
| 创建测试环境存储资源 | 1. S3 测试桶（配置基础加密、版本控制，开放测试 IP 访问）<br>2. R2 测试桶（目录结构与 S3 一致，配置 CORS 允许网页端域名访问） | 前端项目可连接测试桶调试上传逻辑，避免直接操作生产资源                |
| 提供测试密钥      | 1. S3 测试用 AccessKey/SecretAccessKey（权限：仅测试桶写入 + 日志读取）<br>2. R2 测试用 AccessKey/SecretAccessKey（权限：仅测试桶写入） | 前端调用 S3/R2 SDK 时传入密钥，完成身份认证                          |

### 阶段 3：核心功能开发（2-3 周）

按 “上传流程” 拆解模块开发，每个模块需同步与存储团队确认接口细节：

#### 模块 1：上传前准备（1 天）

* 功能：供应商登录（校验 Token 有效性）→ 输入供应商代码→ 下拉选择游戏名（游戏列表由后端接口提供，需与存储目录 “游戏名” 一致）→ 生成唯一 “上传编码”（格式：SUP001_原神_20241001_123456，用于断点续传匹配）；

* 存储团队配合：无（仅需确保 “供应商代码 + 游戏名” 与测试桶目录一致）。

#### 模块 2：文件选择与基础校验（2 天）

* 功能：

  1. 支持批量选择 .mp4/.jsonl/.ini 文件，自动过滤非指定格式文件；

  2. 校验文件名格式（如 .mp4 需符合 [游戏名]_[YYYYMMDD]_[HHMMSS]_[片段序号].mp4），格式错误提示 “请按规范命名文件”；

  3. 关联匹配：自动识别 .mp4 与 .jsonl 文件名（如 原神_20240925_143000_01.mp4 对应 原神_20240925_143000_01.jsonl），缺失对应文件提示 “请补充配套数据文件”；

* 存储团队配合：无（纯前端逻辑，基于已确认的命名规范）。

#### 模块 3：分块上传与断点续传（3-4 天）

* 功能：

  1. 大文件分块：单个文件＞100MB 时自动分片（分片大小 100MB / 片，可配置），计算每片 MD5；

  2. 断点续传核心逻辑：

     * 上传前调用 S3/R2 “列出已上传分片” 接口（如 S3 的 listParts），获取已传分片编号；

     * 仅上传未完成的分片，避免重复传输；

     * 所有分片上传完成后，调用 “合并分片” 接口（如 S3 的 completeMultipartUpload）；

  3. 上传进度展示：实时显示单个文件上传进度、总进度，支持暂停 / 继续上传；

#### 存储团队配合事项

| 配合内容          | 交付物                                                                 | 用途                                                                 |
|-------------------|------------------------------------------------------------------------|----------------------------------------------------------------------|
| 提供分块上传 API 文档 | 1. S3 分块上传接口参数（createMultipartUpload/uploadPart/completeMultipartUpload 接口格式、必填参数、超时时间）<br>2. R2 分块上传接口与 S3 的差异说明（如接口名、返回字段格式） | 前端封装分块上传逻辑，确保与存储层接口兼容；处理 S3/R2 接口差异，避免迁移时重构代码 |
| 确认断点续传匹配规则 | 明确 “上传编码” 是否可作为 S3/R2 分片上传的 “Upload ID” 关联标识，或需额外存储 “上传编码 - Upload ID” 映射 | 确保前端能通过 “上传编码” 准确匹配未完成的分片任务                  |

#### 模块 4：MD5 自动校验（1-2 天）

* 功能：

  1. 前端计算：使用 spark-md5 分片计算本地文件 MD5（避免大文件占用内存过高），生成 local_md5；

  2. 云端校验：文件上传完成后，调用 S3/R2 “获取文件元数据” 接口（如 S3 的 headObject），获取云端计算的 MD5（etag 字段，需确认 S3/R2 是否返回标准 MD5）；

  3. 结果处理：local_md5 与云端 MD5 一致→ 标记 “上传成功”；不一致→ 提示 “校验失败，请重新上传”，支持一键重试；

#### 存储团队配合事项

| 配合内容          | 交付物                                                                 | 用途                                                                 |
|-------------------|------------------------------------------------------------------------|----------------------------------------------------------------------|
| 确认云端 MD5 获取方式 | 1. S3 中文件 MD5 对应的元数据字段（如 ETag 是否为文件完整 MD5，分块上传时 ETag 格式是否特殊）<br>2. R2 中 MD5 字段与 S3 的差异（如是否需额外请求头） | 前端准确提取云端 MD5，避免校验逻辑错误（如分块上传后 ETag 带 -分片数 后缀，需处理） |

#### 模块 5：上传日志查询与校验报告下载（1-2 天）

* 功能：

  1. 日志查询：供应商输入 “供应商代码 + 日期”，前端调用 S3 “读取日志文件” 接口（路径：game-recording-main-apse2/UploadLogs/[供应商代码]/[YYYYMMDD].log），解析日志内容并表格展示；

  2. 校验报告下载：上传成功后，生成含 “文件名、本地 MD5、云端 MD5、校验状态、上传时间” 的 CSV 报告，支持下载；

#### 存储团队配合事项

| 配合内容          | 交付物                                                                 | 用途                                                                 |
|-------------------|------------------------------------------------------------------------|----------------------------------------------------------------------|
| 提供日志文件格式与访问权限 | 1. 日志文件的字段分隔符（如逗号 / 制表符）、编码格式（UTF-8）<br>2. 配置测试桶日志目录权限（允许测试密钥读取本供应商日志文件） | 前端正确解析日志内容，确保供应商仅能查看自己的日志，无越权访问        |

### 阶段 4：联调测试（1 周）

#### 核心任务

1. 功能测试：

   * 正常流程：完整上传 1 个小文件（＜100MB）、1 个大文件（＞1GB），验证 MD5 校验、日志生成、报告下载；

   * 异常流程：断网后重试上传、重复上传同名文件、上传格式错误文件，验证异常提示与处理逻辑；

2. 兼容性测试：适配主流浏览器（Chrome 110+/Edge 110+/Firefox 109+）；

3. 性能测试：同时上传 5 个 1GB 文件，测试进度展示流畅度、无卡顿；

4. R2→S3 迁移测试（若采用 R2 中转）：

   * 前端上传文件至 R2 测试桶；

   * 触发存储团队提供的 “R2→S3 迁移工具”；

   * 验证 S3 测试桶中是否同步出现文件，且 MD5 一致。

#### 存储团队配合事项

| 配合内容          | 交付物                                                                 | 用途                                                                 |
|-------------------|------------------------------------------------------------------------|----------------------------------------------------------------------|
| 提供联调支持      | 1. 测试桶实时监控权限（允许查看前端上传的文件、分片状态）<br>2. 迁移工具测试版本（含触发方式：手动触发 / 定时触发）<br>3. 异常排查协助（如前端上传报 “权限不足”，协助定位桶策略问题） | 快速定位联调中的存储层问题（如分片合并失败、日志未生成），避免卡壳  |
| 确认生产环境预配置 | 1. 生产 S3/R2 桶名、区域、CORS 配置<br>2. 生产密钥的权限范围（仅允许写入指定目录、读取本供应商日志） | 提前对齐生产环境参数，减少上线前的配置调整                            |

### 阶段 5：上线与运维（0.5 周）

#### 核心任务

1. 环境部署：将前端项目打包部署至生产服务器（如 Nginx），配置 HTTPS；

2. 生产参数替换：将环境变量中的 “测试密钥 / 桶名” 替换为存储团队提供的 “生产密钥 / 桶名”；

3. 上线前检查：确认生产桶权限、CORS 配置、日志目录已就绪；

4. 运维文档输出：《网页端上传操作手册》（给供应商）、《常见问题排查指南》（含存储层相关问题，如 “上传失败提示 403” 解决方案）。

#### 存储团队配合事项

| 配合内容          | 交付物                                                                 | 用途                                                                 |
|-------------------|------------------------------------------------------------------------|----------------------------------------------------------------------|
| 提供生产环境资源  | 1. 生产 S3 桶（game-recording-main-apse2）、R2 桶（game-recording-transit-r2）访问权限确认<br>2. 生产用 AccessKey/SecretAccessKey（权限最小化：仅写入 [供应商代码]/[游戏名] 目录、读取本供应商日志） | 前端项目上线后可正常对接生产存储，完成供应商实际数据上传              |
| 运维支持          | 1. 生产桶监控告警配置（如桶容量不足、迁移失败时告警）<br>2. 紧急问题响应通道（如前端报 “生产桶上传超时”，存储团队协助排查网络 / 桶负载） | 保障上线后存储层稳定，快速处理突发问题                                |

## 二、存储团队核心配合事项汇总（按阶段归类）

| 开发阶段          | 配合事项分类 | 具体内容                                                                 | 交付物示例                                                                 |
|-------------------|--------------|--------------------------------------------------------------------------|----------------------------------------------------------------------------|
| 需求分析 / 环境搭建 | 基础资源提供 | 1. S3/R2 测试 / 生产桶名、区域、目录结构<br>2. 测试 / 生产 AccessKey/SecretAccessKey（权限最小化） | 测试桶名：game-recording-test-apse2；测试密钥：AKIAXXXX/XXXXXXX             |
| 核心功能开发      | 接口与规则确认 | 1. S3/R2 分块上传 API 文档、MD5 字段说明<br>2. 断点续传匹配规则（上传编码与 Upload ID 关联方式）<br>3. 日志文件格式与访问权限 | S3 分块上传接口文档；日志字段：上传时间,供应商代码,文件名,MD5,状态          |
| 联调测试          | 问题排查与工具支持 | 1. 测试桶实时监控权限<br>2. R2→S3 迁移测试工具<br>3. 异常问题（如 403/500）协助定位 | 迁移工具手动触发链接；测试桶监控面板地址                                    |
| 上线运维          | 生产环境保障 | 1. 生产桶权限、CORS 配置确认<br>2. 运维监控告警配置<br>3. 紧急问题响应通道 | 生产桶 CORS 配置截图；告警接收邮箱 / 群聊                                   |

## 三、关键风险与应对

| 风险点                  | 应对方案                                                                 | 依赖存储团队配合                                                                 |
|-------------------------|--------------------------------------------------------------------------|----------------------------------------------------------------------------------|
| S3/R2 分块上传接口差异导致代码重构 | 开发前让存储团队提供 S3/R2 接口差异文档，前端封装 “统一上传适配层”（屏蔽底层差异） | 提供 S3/R2 接口差异对比表                                                       |
| 生产密钥泄露风险        | 1. 前端不存储密钥，通过后端接口动态获取（密钥有效期 1 小时）<br>2. 存储团队配置密钥权限：仅允许写入指定目录，无删除 / 列表权限 | 配置密钥最小权限；支持密钥动态生成 / 过期                                       |
| R2→S3 迁移延迟导致数据不一致 | 存储团队配置迁移告警（迁移超时＞1 小时告警），前端显示 “数据迁移中” 状态，提醒供应商待迁移完成后再确认 | 迁移超时告警配置；迁移状态查询接口                                               |