# 游戏录屏数据存储与上传需求文档（面向存储团队）

## 一、文档目的

1. 对齐 AWS S3 悉尼区域 存储配置、目录结构、权限控制等核心技术需求；
2. 明确供应商通过我方自研网页端上传数据的技术支撑要点（含断点续传、MD5 校验、日志留存）；
3. 确认存储层与网页端的对接方案，解决 “供应商直连 S3 悉尼需 VPN” 的潜在问题（如 R2 中转可行性）；
4. 同步待协作事项，确保会议高效推进需求落地。

## 二、需求范围

| 维度     | 具体内容                                                                 |
|----------|--------------------------------------------------------------------------|
| 存储服务 | AWS S3 悉尼区域（AP-Southeast-2，apse2），含主桶配置、权限策略、日志存储<br>可选 中转服务 Cloudflare R2（需评估 “R2 中转→S3 批量迁移” 可行性） |
| 核心用户角色 | 1. 外部供应商：通过网页端上传视频（.mp4）、键鼠数据（.jsonl）、配置文件（.ini）<br>2. 我方运维团队：管理 S3 存储、查看上传日志、处理迁移异常 |
| 关键操作场景 | 供应商数据上传、存储层完整性校验、上传日志追溯、可选 R2→S3 批量迁移       |

## 三、核心需求清单

### （一）存储基础配置需求

| 需求项          | 具体要求                                                                 | 优先级 | 备注                                                                 |
|-----------------|--------------------------------------------------------------------------|--------|----------------------------------------------------------------------|
| S3 主桶创建与区域 | 1. 桶名：game-recording-main-apse2（固定不可修改）<br>2. 区域：AWS 悉尼（AP-Southeast-2，apse2）<br>3. 桶类型：标准桶（支持高并发上传） | P0（必须） | 需存储团队确认桶创建完成时间，及是否需提前配置跨区域访问策略          |
| 安全配置        | 1. 服务器端加密：启用 SSE-S3（AES-256 加密），确保数据静态安全<br>2. 版本控制：启用，防止文件误删 / 覆盖（需保留至少 3 个历史版本）<br>3. 公开访问控制：默认拒绝所有公开读 / 写权限，禁止桶内文件被匿名访问 | P0（必须） | 需配置桶策略，仅允许 “我方自研网页端” 作为合法写入来源                |
| 存储生命周期（优化项） | 1. 非活跃数据迁移：30 天内无访问的文件，从 “标准存储” 自动转 “标准 - IA”<br>2. 过期数据处理：无特殊需求时，数据永久保留（需删除时需审批） | P2（优化） | 需确认生命周期规则配置对现有上传流程的影响                            |

### （二）目录结构与文件规范需求

| 需求项          | 具体要求                                                                 | 优先级 | 备注                                                                 |
|-----------------|--------------------------------------------------------------------------|--------|----------------------------------------------------------------------|
| 目录层级规则    | 仅支持 两层固定结构，禁止供应商自定义子目录：<br>game-recording-main-apse2/[供应商代码]/[游戏名]<br>示例：game-recording-main-apse2/SUP001/原神 | P0（必须） | 需确保 S3 支持中文目录名（UTF-8 编码），避免乱码                      |
| 目录命名约束    | 1. 供应商代码：3-6 位字母 + 数字组合（如 SUP001、SUP123A），由我方提前备案分配，不可修改<br>2. 游戏名：严格匹配官方标准名（如 “原神”“赛博朋克 2077”），需与视频文件名中的 “游戏名” 完全一致 | P0（必须） | 网页端会自动根据 “供应商代码 + 游戏名” 匹配 S3 目录，无需供应商手动输入 |
| 文件格式与命名规范 | 1. 视频文件：后缀 .mp4，命名规则 [游戏名]_[YYYYMMDD]_[HHMMSS]_[片段序号].mp4（如原神_20240925_143000_01.mp4）<br>2. 键鼠数据文件：后缀 .jsonl，与对应视频同名（如原神_20240925_143000_01.jsonl）<br>3. 配置文件（可选）：后缀 .ini，命名规则 [供应商代码]_[YYYYMMDD]_config.ini（如SUP001_20240925_config.ini） | P0（必须） | 单文件最大支持 100GB（适配大视频上传需求，需 S3 支持分块上传）        |

### （三）上传流程技术支撑需求

| 需求项          | 具体要求                                                                 | 优先级 | 备注                                                                 |
|-----------------|--------------------------------------------------------------------------|--------|----------------------------------------------------------------------|
| 网页端与 S3 对接方案 | 1. 对接方式：需提供安全可控的写入方案（如预签名 URL、IAM 角色授权），禁止硬编码密钥<br>2. 目录自动匹配：网页端根据 “供应商代码 + 游戏名” 自动定位 S3 目录，无需供应商手动指定路径<br>3. 批量上传：支持同时选择多个.mp4 和.jsonl 文件（需一一对应），单次上传文件数量无上限（仅受浏览器性能限制） | P0（必须） | 需存储团队提供对接技术文档，明确网页端需集成的 S3 API（如分块上传 API） |
| 断点续传功能    | 1. 支持场景：网络中断（断网、浏览器关闭）、文件上传一半失败后，重新上传无需从头开始<br>2. 匹配逻辑：重新上传时，通过 “供应商代码 + 游戏名 + 上传编码”（网页端生成的唯一标识）匹配未完成文件<br>3. 断点粒度：分块大小建议 100MB（平衡重试效率与请求次数） | P0（必须） | 需确认 S3 原生分块上传 API 是否支持断点续传，或需额外开发状态记录逻辑 |
| R2 中转方案评估（待确认） | 1. 背景：供应商直连 S3 悉尼需配置 VPN，操作成本高，需评估 “R2 中转” 可行性<br>2. 核心需求：<br> - R2 桶创建：目录结构与 S3 完全对齐（[供应商代码]/[游戏名]）<br> - 迁移工具：支持 R2→S3 批量迁移（含 MD5 校验、失败重试 3 次、迁移日志）<br> - 生命周期：R2 中文件迁移成功后 7 天自动删除，失败文件保留 14 天待人工处理<br>3. 成本与性能：评估 R2 存储成本、迁移带宽成本、跨区域迁移耗时 | P1（重要） | 需存储团队判断技术可行性、迁移方案复杂度及落地周期                    |

### （四）校验与日志需求

| 需求项          | 具体要求                                                                 | 优先级 | 备注                                                                 |
|-----------------|--------------------------------------------------------------------------|--------|----------------------------------------------------------------------|
| MD5 自动校验    | 1. 校验流程：<br> - 网页端上传前自动计算本地文件 MD5 值（无需供应商手动操作）<br> - 文件上传至 S3/R2 后，云端自动重新计算 MD5 值并与本地值比对<br> - 校验一致→显示 “上传成功”；不一致→显示 “校验失败”，提示重新上传<br>2. 校验报告：上传成功后，网页端支持下载 “文件名、本地 MD5、云端 MD5、校验状态” 的报告 | P0（必须） | 需确认 S3/R2 是否原生支持 MD5 值返回，或需额外开发校验逻辑            |
| 上传日志存储与查询 | 1. 日志内容（必填）：<br> - 基础信息：上传时间（YYYY-MM-DD HH:MM:SS）、供应商代码、游戏名、上传编码<br> - 文件信息：文件名、文件大小（MB）、本地 MD5、云端 MD5、校验状态（成功 / 失败）、上传耗时（秒）<br> - 操作人信息：网页端登录账号（与供应商代码绑定）<br>2. 存储路径：game-recording-main-apse2/UploadLogs/[供应商代码]/[YYYYMMDD].log（S3 中）<br>3. 留存期限：至少 6 个月<br>4. 查询权限：<br> - 我方运维：可查看所有供应商日志，支持下载<br> - 供应商：仅可查看本团队日志，无下载权限 | P0（必须） | 需确认 S3 是否支持日志自动写入指定目录，或需通过 Lambda 函数同步日志  |

### （五）权限控制需求

| 需求项          | 具体要求                                                                 | 优先级 | 备注                                                                 |
|-----------------|--------------------------------------------------------------------------|--------|----------------------------------------------------------------------|
| 写入权限控制    | 1. 仅允许 “我方自研网页端” 向 S3/R2 写入文件，禁止其他来源（如 AWS 控制台、S3 CLI、第三方工具）<br>2. 限制方式：通过桶策略绑定网页端服务器 IP / 域名，或通过 IAM 角色仅授权网页端服务账号 | P0（必须） | 需存储团队提供桶策略配置示例，确保权限最小化                          |
| 权限分级        | 1. 我方运维团队：S3 全权限（读、写、删、配置、日志查询）<br>2. 供应商：仅拥有 “通过网页端上传文件” 权限，无 S3/R2 直接访问权限<br>3. 开发 / 测试团队：仅拥有指定测试目录的访问权限 | P0（必须） | 需配置 IAM 角色与桶策略联动，避免权限泄露                              |

## 四、待会议协作确认事项（重点议题）

1. S3 主桶落地：game-recording-main-apse2的创建进度、完成时间，及基础配置（加密、版本控制）的操作细节；
2. 网页端对接 S3：推荐的对接方案（预签名 URL vs IAM 角色）、分块上传参数（分块大小、预签名 URL 有效期）；
3. R2 中转可行性：技术方案是否可行、迁移工具的开发 / 选型建议、成本与性能评估结果；
4. MD5 校验实现：S3/R2 是否原生支持 MD5 值返回，若不支持，需如何开发补充；
5. 日志生成逻辑：S3 是否可自动生成符合需求的上传日志，或需通过 Lambda 函数 / 其他工具同步日志；
6. 权限配置示例：提供 “仅允许网页端写入” 的桶策略配置模板，及 IAM 角色权限分配示例；
7. 大文件上传兼容性：S3 对 100GB 单文件上传的支持情况，是否需调整桶的分块上传阈值。

## 五、预期输出

1. 确认 S3 主桶game-recording-main-apse2的创建完成时间；
2. 输出 “网页端与 S3 对接” 的技术方案文档（含 API 列表、配置步骤）；
3. 明确 R2 中转方案的可行性结论及落地计划（若采用）；
4. 提供桶策略、IAM 权限配置示例；
5. 对齐校验日志的实现路径（原生支持 vs 额外开发）；
6. 确认关键时间节点（如 S3 配置完成、对接测试启动、全流程联调）。

## 六、时间节点规划（待对齐）

| 事项                          | 预计完成时间 | 负责人              | 依赖项                  |
|-------------------------------|--------------|---------------------|-------------------------|
| S3 主桶创建与基础配置         | -            | 存储团队            | -                       |
| 网页端与 S3 对接方案确认      | -            | 存储团队 + 开发团队 | S3 主桶配置完成         |
| R2 中转方案评估与确认         | -            | 存储团队            | -                       |
| 权限策略与日志配置            | -            | 存储团队            | S3 主桶创建完成         |
| 网页端与 S3 对接测试          | -            | 开发团队 + 存储团队 | 对接方案确认            |
| R2→S3 迁移工具开发（若采用）  | -            | 存储团队            | R2 方案确认             |
| 全流程联调（上传 + 校验 + 日志） | -            | 开发 + 存储 + 测试团队 | 所有前置配置完成        |