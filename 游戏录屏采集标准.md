# 游戏录屏采集标准 20250926

## 1. 引言
为明确游戏录屏采集的采集标准与交付流程，推出此MD文档。

## 2. 基础环境与兼容性要求  

### 2.1 系统环境  
#### 2.1.1 操作系统版本要求  
- **支持系统**：  
  - Windows 10 64位（专业版/企业版，版本号≥1903）  
  - Windows 11 64位（专业版/企业版，版本号≥21H2）  
- **排除范围**：  
  - 32位Windows系统（不支持64位exe程序）  
  - Linux/macOS系统（exe程序依赖Windows环境）  
  - Windows家庭版（部分组策略限制可能导致OBS性能不稳定）  

#### 2.1.2 硬件配置最低标准  
| 硬件类型       | 最低配置                          | 推荐配置                          | 备注                                  |  
|----------------|-----------------------------------|-----------------------------------|---------------------------------------|  
| CPU            | Intel i5-8400 / AMD Ryzen 5 2600 | Intel i7-10700 / AMD Ryzen 7 5800X | 确保多线程采集（视频+输入）无卡顿    |  
| 内存           | 8GB DDR4 2666MHz                  | 16GB DDR4 3200MHz                 | 避免录制高分辨率视频时内存溢出        |  
| 存储           | 100GB 空闲空间（SSD/HDD）         | 500GB 空闲空间（NVMe SSD）        | SSD优先：提升视频写入速度，减少丢帧  |  
| 显卡           | Intel UHD 630 / NVIDIA GTX 1050   | NVIDIA RTX 3060 / AMD RX 6600     | 支持硬件编码（NVENC/AMF），降低CPU负载 |  
| 网络（可选）   | 100Mbps 有线网络                  | 1Gbps 有线网络                    | 仅用于数据上传阶段，本地采集无强制要求 |  


### 2.2 软件依赖  
#### 2.2.1 必备软件版本清单  
| 软件名称              | 版本要求          | 作用                                  | 安装说明                                                             |  
|-----------------------|-------------------|---------------------------------------|------------------------------------------------------------------|  
| OBS Studio            | ≥27.0.0（64位）   | 视频采集核心工具                      | 从官网下载：https://obsproject.com/                                    |  
| 自研输入采集程序      | obs_tracker_V2.1  | 键鼠手柄操作记录                      | 从GitHub获取（提供独立exe文件，无需安装）：https://github.com/Ayreh/ScreenRecorder|  

#### 2.2.2 关键插件与内置组件  
| 组件名称              | 版本要求          | 所属程序         | 说明                                  |  
|-----------------------|-------------------|------------------|---------------------------------------|  
| obs-websocket         | ≥5.0.1            | OBS Studio       | 需手动启用（见2.2.3），用于OBS与自研程序的状态同步 |  
| （内置）pynput        | 1.7.6             | 自研exe程序      | 已封装在exe中，用于捕获鼠标/键盘事件  |  
| （内置）pygame        | 2.1.0             | 自研exe程序      | 已封装在exe中，用于捕获手柄事件        |  
| （内置）obsws-python  | 1.2.0             | 自研exe程序      | 已封装在exe中，用于解析OBS WebSocket协议 |  

#### 2.2.3 配置验证  
- **OBS配置**：  
  打开OBS → 工具 → WebSocket Server Settings → 勾选“Enable WebSocket server”，记录端口（默认4455）和密码（若设置），点击“Apply”保存。  
- **自研程序验证**：  
  从GitHub下载`obs_tracker_V2.1.exe` → 双击运行 → 程序自动读取系统设备信息，在界面显示“初始化完成”即表示正常启动（首次运行会生成配置文件，路径：`C:\Users\[用户名]\AppData\Local\OBSTracker\config.ini`）。  


### 2.3 设备兼容  
#### 2.3.1 键盘与鼠标  
- **支持类型**：  
  - 键盘：所有USB/PS/2接口的有线键盘、蓝牙无线键盘（需适配Windows驱动），包括机械键盘、薄膜键盘、静电容键盘。  
  - 鼠标：所有USB/蓝牙连接的鼠标（支持滚轮、侧键），包括有线鼠标、2.4G无线鼠标（需配套接收器正常工作）。  
- **不支持情况**：  
  - 未安装驱动的自定义宏键盘（可能导致键码识别异常）。  
  - 鼠标DPI动态切换功能（仅记录当前DPI，不追踪切换事件）。  

#### 2.3.2 游戏手柄  
- **支持型号**：  
  - 微软：Xbox Series X/S手柄、Xbox One手柄（有线/蓝牙）。  
  - 索尼：PS5 DualSense手柄、PS4 DualShock 4手柄（需安装DS4Windows驱动）。  
  - 任天堂：Switch Pro手柄（有线/蓝牙）。  
  - 通用：符合DirectInput/XInput标准的USB手柄（如北通、雷蛇系列）。  
- **连接方式**：支持USB有线连接、蓝牙无线连接（需确保Windows蓝牙驱动正常）。  
- **限制**：  
  - 仅支持单手柄记录（默认追踪系统识别的第一个手柄）。  
  - 不支持手柄震动反馈记录（仅记录输入，不记录输出）。  

#### 2.3.3 设备验证  
- 键鼠：接入设备后，在“设备管理器”中确认无黄色感叹号（驱动正常），通过`keyboardtester.com`验证按键响应。  
- 手柄：接入后，通过“控制面板→设备和打印机”确认手柄被识别，运行自研程序后查看界面“设备状态”栏显示“手柄已连接”即表示正常。


## 3. 采集软件规范  

### 3.1 视频采集软件  
#### 3.1.1 选型标准  
- **软件名称**：OBS Studio（开源免费）  
- **选型依据**：  
  - 支持高分辨率、高帧率视频采集，兼容主流游戏渲染模式（DirectX/OpenGL/Vulkan）；  
  - 内置WebSocket接口，可与自研输入采集程序实时通信，确保录制状态同步；  
  - 支持硬件编码（NVENC/AMF/Quick Sync），平衡画质与性能消耗；  
  - 社区活跃，版本更新及时，兼容Windows 10/11系统。  
- **版本要求**：≥27.0.0（64位），推荐使用29.1.3稳定版（经兼容性测试验证）。  

#### 3.1.2 核心配置要求  
| 配置类别       | 具体要求                                                                                                                             | 配置路径                                  |  
|----------------|----------------------------------------------------------------------------------------------------------------------------------|-------------------------------------------|  
| WebSocket设置  | - 启用WebSocket服务：勾选“Enable WebSocket server”<br>- 端口：默认4455（如需修改，需与自研程序配置一致）<br>- 认证：建议设置密码（≥8位，含数字+字母），并在自研程序中同步配置               | OBS → 工具 → WebSocket Server Settings    |  
| 录屏源设置     | - 优先使用“游戏捕获”源：选择“模式→捕获特定窗口”，指定目标游戏进程（如“GenshinImpact.exe”）<br>- 备选方案：“窗口捕获”（针对非全屏游戏）或“显示器捕获”（不推荐，易含冗余内容）<br>- 禁用“预览窗口”：降低GPU资源占用 | OBS → 场景集合 → 添加源 → 游戏捕获        |  
| 编码设置       | - 视频编码器：选择“硬件编码”（如NVIDIA NVENC H.264）<br>- 码率控制：CBR（恒定比特率）<br>- 比特率：1920×1080@60fps设为15-20Mbps；2560×1440@60fps设为25-30Mbps        | OBS → 文件 → 设置 → 输出 → 录像          |  
| 输出设置       | - 格式：MP4（兼容主流播放器，支持中断后修复）<br>-  文件名格式：`[YYYY-MM-DD] [HH-MM-SS]`（如“2024-09-25 14-30-00.mp4”）                                      | OBS → 文件 → 设置 → 输出 → 录像          |  
| 性能优化       | - 关闭“垂直同步”：避免画面延迟<br>- 启用“多线程编码”：提升编码效率<br>- 帧率：与游戏实际帧率一致（默认60fps）                                                               | OBS → 文件 → 设置 → 视频 / 输出          |  


### 3.2 输入采集软件  
#### 3.2.1 选型标准  
- **软件名称**：自研OBS陪伴程序（obs_tracker_V2.1，exe格式）  
- **选型依据**：  
  - 专为游戏录屏场景设计，与OBS WebSocket深度适配，实现毫秒级状态同步；  
  - 集成键鼠手柄全类型事件采集功能，数据格式与视频文件强关联（同名JSONL）；  
  - 封装为单文件exe，无需安装依赖，双击即可运行，降低使用门槛；  
  - 开源可追溯（GitHub仓库：https://github.com/Ayreh/ScreenRecorder)

#### 3.2.2 功能要求  
##### 3.2.2.1 状态同步功能  
- **同步触发机制**：  
  - 启动后点击连接OBS WebSocket（需配置正确的地址、端口、密码）；  
  - 实时监听OBS录制状态：当OBS触发“开始录制”时，自动启动键鼠手柄事件采集；当OBS触发“停止录制”时，同步停止采集并生成完整性标识。  
- **同步精度**：事件时间戳与OBS录制时间戳偏差≤1ms（通过WebSocket协议实时校准）。  
- **异常同步处理**：  
  - 若OBS崩溃，程序检测到连接中断后，自动停止采集并保存已记录数据；  
  - 若网络波动导致WebSocket短暂断开（≤5s），重连后自动续接采集（时间戳无缝衔接）。  

##### 3.2.2.2 事件采集范围  
| 设备类型 | 采集对象                  | 核心记录字段                                                                 | 采集频率/触发条件                     |  
|----------|---------------------------|------------------------------------------------------------------------------|---------------------------------------|  
| 鼠标     | 位移                      | X/Y轴原始物理位移（硬件脉冲数，正/负值表示方向）                             | 实时（鼠标移动时触发）                |  
|          | 按键                      | 按键类型（left/right/middle/side1/side2）、状态（press/release）             | 按键动作触发                          |  
|          | 滚轮                      | 滚动方向（正向+1/反向-1，基于硬件脉冲）                                      | 滚轮滚动时触发                        |  
| 键盘     | 按键                      | Windows虚拟键码（VK_CODE，如A=0x41、Shift=0x10）、状态（press/release）      | 按键动作触发（无字符映射，规避输入法）|  
| 手柄     | 动作按键                  | 按键名称（a_button/b_button/x_button/y_button等）、原始键码、状态（press/release） | 按键动作触发                          |  
|          | 肩键/特殊键               | 按键名称（lb_shoulder/rb_shoulder/back/start等）、状态（press/release）       | 按键动作触发                          |  
|          | 摇杆                      | 摇杆类型（left_stick/right_stick）、轴方向（x/y）、原始值（-32768~32767）、归一化值（-1.0~1.0） | 30Hz固定采样（仅记录＞0.1归一化值的位移，过滤抖动） |  
|          | 扳机                      | 扳机类型（left_trigger/right_trigger）、压力值（0~1023，0为完全释放）         | 30Hz固定采样（值变化时记录）          |  
|          | 方向键（D-pad）           | 方向描述（up/down/left/right/neutral等）、原始X/Y坐标（-1/0/1）               | 方向变化时触发                        |  

##### 3.2.2.3 数据输出要求  
- **文件格式**：JSONL（每行一个事件，UTF-8编码），与对应视频文件同名（仅后缀为`.jsonl`）；  
- **元数据**：文件首行包含设备信息、录制起止时间、软件版本等基础信息（见4.2.2）；  
- **完整性保障**：录制结束后自动追加`"event_type": "SessionEnd"`标识，用于校验文件未损坏。  

## 4. 交付内容标准  


### 4.1 视频交付标准  
#### 4.1.1 基础参数规范  
| 参数类别       | 技术要求                                                                 | 说明                                                                 |  
|----------------|--------------------------------------------------------------------------|----------------------------------------------------------------------|  
| 分辨率         | 优先支持：1920×1080（1080P）<br>可选支持：2560×1440（2K）、1280×720（720P） | 需与游戏实际运行分辨率一致，禁止拉伸或缩放（避免画面变形）；同一批次录制需保持分辨率统一 |  
| 帧率           | 固定60fps（最低不低于30fps）                                             | 确保游戏操作流畅性，动作类游戏必须满足60fps；帧率波动≤±1fps（通过OBS“帧率计数器”监控） |  
| 编码格式       | 视频：H.264（High Profile，Level 4.1）<br>音频：AAC（LC Profile）         | 视频码率控制采用CBR（恒定比特率），音频采样率48kHz，声道2.0（立体声）   |  
| 比特率         | 1080P@60fps：15-20Mbps<br>2K@60fps：25-30Mbps<br>720P@60fps：8-12Mbps    | 平衡画质与文件大小，避免因码率过低导致画面模糊（尤其是快速移动场景）   |  
| 色彩空间       | Rec.709（标准高清色彩空间）                                               | 确保色彩还原准确，与游戏原始画面一致                                 |  


#### 4.1.2 文件规范  

- **命名规则**：`[YYYY-MM-DD] [HH-MM-SS].mp4`  
- **示例**：  
  - 2024年9月25日14:30开始录制：`2024-09-25 14-30-00.mp4`
- **约束**：
  - 日期、时间精确到秒，与OBS内部录制启动时间一致；


### 4.2 操作数据交付标准  
#### 4.2.1 格式要求  
- **文件格式**：JSON Lines（JSONL），即每行一个独立JSON对象，无嵌套数组。  
- **编码方式**：UTF-8（确保中文设备名称、方向描述等字符正常显示）。  
- **文件大小**：随视频文件同步分割（与对应视频片段大小匹配），单个JSONL文件大小通常为对应视频文件的1%-5%（取决于操作密度）。  
- **命名规则**：与对应视频文件的时间对应，后缀为`.jsonl`（示例：`20240925_143000.jsonl`）。  


#### 4.2.2 核心字段规范  
##### 4.2.2.1 元数据（文件首行）  
元数据为固定格式，包含基础信息用于溯源，示例：  
```json
{
  "timestamp": 7.2e-05, 
  "absolute_time": 1758526531.944443, 
  "event_type": "SessionStart", 
  "details": 
  {
    "obs_sync": true,
    "start_time_unix": 1758526531.9443312, 
    "start_time_readable": "2025-09-22T15:35:31.944331", 
    "obs_connected": true, 
    "pynput_available": true,
    "gamepad_enabled": true, 
    "pygame_available": true,
    "gamepad_count": 0
  }
}
```
#### 4.2.2.2 事件字段（按类型区分）

所有事件需包含 `obs_timestamp`（与OBS视频帧时间戳对齐）和 `type`（事件类型），具体规范如下：

| 事件类型   | 核心字段说明                                                                 | 示例（JSONL格式）                                                                 |
|------------|------------------------------------------------------------------------------|-----------------------------------------------------------------------------------|
| 鼠标位移   | - `type`: `"mouse_move"`（固定值）<br>- `data.dx`: X轴位移脉冲（整数，正/负表示方向）<br>- `data.dy`: Y轴位移脉冲（整数，正/负表示方向） | ```json<br>{"obs_timestamp": 1727236201000, "type": "mouse_move", "data": {"dx": 15, "dy": -8}}<br>``` |
| 鼠标按键   | - `type`: `"mouse_button"`（固定值）<br>- `data.button`: 按键类型（`left`/`right`/`middle`/`side1`/`side2`）<br>- `data.state`: 状态（`press`/`release`） | ```json<br>{"obs_timestamp": 1727236202000, "type": "mouse_button", "data": {"button": "left", "state": "press"}}<br>``` |
| 鼠标滚轮   | - `type`: `"mouse_wheel"`（固定值）<br>- `data.delta`: 滚动脉冲（`+1`向前，`-1`向后） | ```json<br>{"obs_timestamp": 1727236203000, "type": "mouse_wheel", "data": {"delta": 1}}<br>``` |
| 键盘事件   | - `type`: `"keyboard"`（固定值）<br>- `data.vk_code`: Windows虚拟键码（十六进制，如`0x41`）<br>- `data.state`: 状态（`press`/`release`） | ```json<br>{"obs_timestamp": 1727236204000, "type": "keyboard", "data": {"vk_code": 0x41, "state": "press"}}<br>``` |
| 手柄按钮   | - `type`: `"gamepad_button"`（固定值）<br>- `data.button`: 按钮名称（`a_button`/`b_button`/`x_button`/`y_button`等）<br>- `data.raw_code`: 硬件原始键码（整数）<br>- `data.state`: 状态（`press`/`release`） | ```json<br>{"obs_timestamp": 1727236205000, "type": "gamepad_button", "data": {"button": "a_button", "raw_code": 0, "state": "press"}}<br>``` |
| 手柄摇杆   | - `type`: `"gamepad_stick"`（固定值）<br>- `data.stick`: 摇杆类型（`left_stick`/`right_stick`）<br>- `data.axis`: 轴方向（`x`/`y`）<br>- `data.value`: 原始值（`-32768`~`32767`）<br>- `data.normalized`: 归一化值（`-1.0`~`1.0`） | ```json<br>{"obs_timestamp": 1727236206000, "type": "gamepad_stick", "data": {"stick": "left_stick", "axis": "x", "value": 15600, "normalized": 0.48}}<br>``` |
| 手柄扳机   | - `type`: `"gamepad_trigger"`（固定值）<br>- `data.trigger`: 扳机类型（`left_trigger`/`right_trigger`）<br>- `data.value`: 压力值（`0`~`1023`） | ```json<br>{"obs_timestamp": 1727236207000, "type": "gamepad_trigger", "data": {"trigger": "right_trigger", "value": 780}}<br>``` |
| 手柄方向键 | - `type`: `"gamepad_dpad"`（固定值）<br>- `data.direction`: 方向（`up`/`down`/`left`/`right`/`up_left`等）<br>- `data.raw_x`: X轴原始值（`-1`/`0`/`1`）<br>- `data.raw_y`: Y轴原始值（`-1`/`0`/`1`） | ```json<br>{"obs_timestamp": 1727236208000, "type": "gamepad_dpad", "data": {"direction": "up", "raw_x": 0, "raw_y": 1}}<br>``` |
| 录制结束标识 | - `type`: `"recording_complete"`（固定值）<br>- `data.status`: 状态（固定为`"success"`） | ```json<br>{"obs_timestamp": 1727239800000, "type": "recording_complete", "data": {"status": "success"}}<br>``` |

#### 4.2.2.3 结束字段
元数据为固定格式，包含基础信息用于溯源，示例： 
```json
{
  "timestamp": 44.235763,
  "absolute_time": 1758526576.1776924,
  "event_type": "SessionEnd",
  "details": 
  {
    "total_events": 3064, 
    "mouse_events": 3008, 
    "keyboard_events": 56, 
    "gamepad_events": 0, 
    "duration_seconds": 44.235761099960655
  }
}
```

#### 4.2.2.4 约束条件

1. **时间戳规则**  
   - 所有 `obs_timestamp` 必须为**毫秒级Unix时间戳（整数）**，且需满足：`recording_start_obs_ts ≤ obs_timestamp ≤ recording_end_obs_ts`（误差允许±10ms）。

2. **数值范围约束**  
   - 手柄摇杆：`data.value` 必须在 `[-32768, 32767]` 区间；`data.normalized` 必须在 `[-1.0, 1.0]` 区间，且两者满足 `normalized = value / 32767`（四舍五入保留2位小数）。  
   - 手柄扳机：`data.value` 必须在 `[0, 1023]` 区间（`0`=完全释放，`1023`=完全按下）。  
   - 键盘虚拟键码：`data.vk_code` 必须符合Windows标准VK_CODE定义（范围 `0x00`~`0xFF`）。

3. **字段完整性要求**  
   - 每个事件必须包含 `obs_timestamp` 和 `type` 字段，不可缺失。  
   - 各类型事件的 `data` 子字段需完整包含表格中列出的所有项（例如“鼠标按键”事件必须同时包含 `button` 和 `state`）。

4. **命名规范**  
   - 事件类型 `type` 需严格匹配表格中定义的字符串（区分大小写，如 `"mouse_move"` 不可简写为 `"mousemove"`）。  
   - 设备部件名称（如 `data.button` 中的 `a_button`、`data.stick` 中的 `left_stick`）需从预设枚举列表选取，禁止自定义名称。

## 5. 数据存储规范（R2临时中转 + S3长期存储）  
### 核心逻辑  
采用“双存储层级”：  
- **Cloudflare R2**：作为供应商上传的“临时中转层”，无需VPN，降低上传门槛、提升传输稳定性；  
- **AWS S3（悉尼区域）**：作为“长期存储层”，用于最终数据归档，保障合规性与长期安全性；  
- 关键约束：R2与S3的目录结构完全统一，确保批量迁移无格式冲突。  


### 5.1 双存储层级配置（R2中转 + S3长期）  
#### 5.1.1 临时中转层：Cloudflare R2 配置  
- **区域选择**：默认使用Cloudflare亚太区边缘节点（如“Hong Kong”“Singapore”），优先匹配供应商所在区域，最大化降低上传延迟；  
- **R2桶配置**：  
  1. 统一使用主桶：`game-recording-transit-r2`（由我方管理，供应商无需创建）；  
  2. 安全设置：  
     - 启用“服务器端加密”（AES-256-GCM，与S3加密标准对齐）；  
     - 访问控制：仅开放“我方自研网页上传端”的写入权限，禁止公开访问/其他工具（如R2控制台、CLI）操作；  
     - 防盗链：绑定网页上传端的域名，拒绝非授权域名的文件上传请求；  
  3. 存储生命周期：R2中文件默认保留 **7天**（从上传成功时间起算），若7天内未完成向S3的迁移，系统自动触发告警（通知我方运维）。  

#### 5.1.2 长期存储层：AWS S3 悉尼区域配置
- **区域固定**：AWS悉尼区域（AP-Southeast-2，apse2），用于最终数据归档；  
- **S3桶配置**：  
  1. 主桶：`game-recording-main-apse2`（我方管理，无需供应商操作）；  
  2. 安全设置：启用服务器端加密（SSE-S3）、版本控制（防止迁移后误删/覆盖）、访问权限控制（仅我方迁移工具可写入，禁止供应商直接操作）；  
  3. 与R2联动：仅接收从R2批量迁移的文件，不接受供应商直接上传。  

### 5.2 统一目录结构（R2与S3完全一致）  
为避免迁移时格式冲突，R2与S3采用完全相同的“两层简化目录”，供应商上传至R2的文件结构，将直接同步至S3。结构如下： 

```
game-recording-main-apse2/  # 主桶（固定，不可修改）
├─ [供应商代码]/  # 第一层目录：我方分配的专属供应商代码（格式要求：3-6位字母+数字组合，示例：SUP001、SUP002、SUP123A）
│  ├─ [游戏名]/  # 第二层目录：官方标准游戏名（需与“4. 交付标准”中“游戏名”字段完全一致，示例：“原神”“赛博朋克 2077”“英雄联盟”）
│  │     ├─ [上传代码] #第三层目录：上传批次代码（格式：日期+随机数，示例：20240925_143000）
│  │     │  ├─ 2024-09-25 14-30-00.mp4  # 视频文件（命名需严格遵循“4.1.2 文件名规范”）
│  │     │  ├─ 20240925_143000.jsonl  # 键鼠/手柄数据文件（需与对应视频时间相同，后缀改为.jsonl，一一对应）
│  │     │  └─ [可选] SUP001_20240925_config.ini  # 配置快照文件（按需上传，非必选；命名格式：供应商代码_日期_config.ini）
```

#### 约束规则（R2与S3通用）  
1. 供应商代码：由我方提前备案分配，不可自定义（如SUP001绑定甲供应商，上传时网页端自动关联，无需手动输入）；  
2. 游戏名：严格使用官方标准名称（如“英雄联盟”不可写“LOL”，“赛博朋克 2077”不可省略空格），需与“4.1.2视频文件名”中的“游戏名”完全一致；  
3. 文件存放：视频与JSONL文件直接存于“供应商代码-游戏名”目录下，**禁止创建子文件夹**（如Video/InputData），确保迁移时文件路径无冗余。  

### 5.3 R2→S3 批量迁移规范（我方运维操作，供应商无需参与）  
#### 5.3.1 迁移触发条件  
- **时间触发**：每日凌晨2:00（UTC+8）自动触发迁移，将R2中“供应商代码-游戏名”目录下的文件被确认上传完成（完成6.2 校验流程）的文件迁移至S3，确保R2中的文件不积压；

#### 5.3.2 迁移工具与流程  
1. **迁移工具**：使用Cloudflare Workers（轻量无服务器）+ AWS SDK，避免搭建专用迁移服务器；  
2. **迁移流程**：  
   1. 触发迁移后，Workers自动遍历R2中待迁移的文件（过滤已迁移标记的文件）；  
   2. 通过HTTPS加密传输（规避VPN），将文件批量同步至S3对应目录（按“供应商代码-游戏名”匹配路径）；  
   3. 迁移完成后，在R2文件的“元数据”中添加`migrated:success`标记，避免重复迁移。  

#### 5.3.3 迁移完整性校验  
1. **MD5比对**：迁移前获取R2文件的MD5值，迁移至S3后重新计算S3文件的MD5值，两者一致则判定“迁移成功”；  
2. **日志记录**：每批次迁移生成《迁移报告》，包含：  
   - 基础信息：迁移时间（YYYY-MM-DD HH:MM:SS）、涉及的供应商代码/游戏名、文件总数/总大小；  
   - 明细信息：每个文件的R2路径、S3路径、MD5值（R2/S3）、迁移状态（成功/失败）、失败原因（如网络超时）；  
3. **失败重试**：对迁移失败的文件，自动重试3次（每次间隔5分钟）；若仍失败，触发告警（通知我方运维），人工介入排查。  

#### 5.3.4 R2文件生命周期（迁移后处理）  
- 迁移成功的文件：在R2中保留7天后自动删除（避免占用中转存储）；  
- 迁移失败的文件：标记为`migrated:failed`，保留至人工处理完成后删除，最长保留14天。  

## 6. 数据上传规范（基于 R2bucketuploader 桌面版）（In Progress）
### 6.1 上传工具说明（桌面 EXE 应用）
#### 6.1.1 工具特性与分发方式
* **工具形态**：基于 r2-bucket-uploader 核心逻辑打包的桌面应用（EXE 格式），支持 Windows/macOS 系统，内置 Uppy 上传引擎及分块上传能力，无需依赖浏览器；
* **分发与更新**：
  * 由我方提供加密压缩包（含校验码），供应商通过指定链接下载，解压后即可使用（绿色版，无需安装）；

* **核心优势**：
  * 大文件上传更稳定（不受浏览器内存限制，支持 100GB+ 文件分块传输）；
  * 支持本地文件拖拽（直接从文件夹拖入应用窗口）；
  * 断网后自动重试（无需手动操作，网络恢复后继续上传）。

#### 6.1.2 前置准备（上传代码获取）
与网页端一致，需通过飞书多维表格获取上传代码：
1. 访问我方共享的飞书多维表格（链接：[我方提供的表格地址]）；
2. 填写表单字段：
   * 供应商代码（如 SUP001，需与我方分配值一致）；
   * 游戏名（下拉选择，与 5.2 目录中 “游戏名” 一致，新游戏可手动输入）；
   * 用户名（选择飞书通讯录本人账号，用于统计上传时长及结算）；
3. 提交后，表格自动生成位上传代码，显示有效期（默认 72 小时）及状态，复制代码备用。
### 6.2 核心操作流程（桌面版应用）
#### 6.2.1 应用启动与配置
1. 解压下载的应用包，双击 R2Uploader.exe 启动（首次启动会生成本地配置文件夹，用于存储日志和缓存）；
2. 首次使用需完成基础配置（仅需一次）：
   * 语言选择（默认中文）；
   * 本地缓存路径设置（建议选择非系统盘，用于临时存储分块文件，空间需≥计划上传的最大文件）；

#### 6.2.2 上传操作步骤
1. 输入验证信息：在应用主界面的 “上传配置” 区域，填写：
   * 供应商代码（如 SUP001）；
   * 游戏名（下拉选择，需与飞书表格填写一致）；
   * 上传代码（粘贴从飞书表格获取的代码）；
     点击 “验证代码”，应用会通过内置 API 校验代码有效性（未过期、未使用、匹配供应商 / 游戏名），校验通过后解锁文件选择区。
2. 文件选择与校验：
   * **方式 1**：点击 “添加文件” 按钮，批量选择对应批次的 .mp4 视频 和 .jsonl 数据文件；
   * **方式 2**：直接从本地文件夹拖拽文件到应用的 “文件列表区”；
   * 应用会自动校验文件名格式（不符合 4.1.2/4.2.1 规范的文件会标红，鼠标悬停显示错误原因，禁止上传）。
3. 启动上传与监控：
   * 点击 “开始上传”，应用自动执行：
     * 大文件分块（≥100MB 的文件自动拆分为 50MB / 块，提升稳定性）；
     * 生成 R2 存储路径：game-recording-main-apse2/[供应商代码]/[游戏名]/[文件名]；
     * 实时显示总进度（百分比）、当前上传文件、分块进度及剩余时间；
   * 上传过程中可最小化应用（后台继续传输），但不可关闭（关闭会中断上传，下次启动需重新验证代码续传）。
4. 完成与确认：
   * 全部文件上传成功且校验通过后，应用会显示 “上传完成” 弹窗，包含本次上传的文件总数、总大小、耗时；
   * 点击 “查看报告” 可生成本地校验报告（txt 格式，含文件名、MD5 比对结果、上传代码），建议保存留档；
   * 飞书表格中对应上传代码的状态会自动更新为 “已使用”，并同步上传结果。

### 6.3 断点续传与异常处理
#### 6.3.1 断点续传机制
* 支持两种场景的续传：
  1. **网络中断**：应用会自动检测网络状态，恢复后无需操作，从上次中断的分块继续上传；
  2. **应用关闭**：重新启动应用，输入相同的供应商代码 + 游戏名 + 上传代码，应用会读取本地缓存的 “未完成文件记录”，自动识别需续传的文件并继续上传。

#### 6.3.2 异常处理
* **文件校验失败**：应用会标记失败文件并显示 “重试” 按钮，点击后仅重传该文件（无需重新上传全部）；
* **上传代码过期**：若上传过程中代码过期（如超过 24 小时），应用会提示 “代码已过期”，需重新从飞书表格获取代码，输入后可继续上传（已上传的分块无需重复传输）；
* **磁盘空间不足**：缓存路径空间不足时，应用会提示 “清理缓存” 或 “更换缓存路径”，清理后不影响已上传的分块（云端已存储）。

### 6.4 日志与数据联动
#### 6.4.1 本地日志与云端同步
* **本地日志**：应用自动在配置文件夹生成日志文件（路径：[缓存路径]/logs/[YYYYMMDD].log），记录：
  * 操作时间、上传代码、供应商代码、游戏名；
  * 每个文件的上传进度、分块成功 / 失败记录、MD5 值、耗时；
* **云端日志**：上传完成后，应用会将核心日志（通过加密接口）同步至 R2 日志目录：game-recording-main-apse2/UploadLogs/[供应商代码]/[YYYYMMDD].log，与网页端日志格式一致。

#### 6.4.2 与飞书表格的联动
* 桌面应用通过内置 API 与飞书表格联动，实现：
  * **上传前**：验证代码时拉取表格数据进行比对；
  * **上传中**：每 90 秒同步一次 “当前上传进度” 至表格（可选）；
  * **上传后**：更新表格状态为 “已使用”，补充 “总文件大小、校验报告本地路径、上传完成时间”；
* 结算时，可通过表格直接汇总 “用户名” 对应的上传数据（无需依赖应用本地文件）。

### 6.5 安全与约束
1. **应用安全性**：
   * 我方提供的 EXE 包会附带 SHA256 校验码，供应商需核对校验码后再使用（防止文件被篡改）；
   * 应用仅通过 HTTPS 与后端通信，所有敏感信息（如上传代码）传输时会加密；
2. **使用约束**：
   * 禁止修改应用目录下的任何文件（如 config.json、core.dll），否则会导致上传失败或被标记为 “异常操作”；
   * 同一上传代码仅支持在一台设备上使用（防止多设备同时上传导致冲突）；
   * 若需在多设备上传，需为每个设备单独生成上传代码（通过飞书表格多次提交）。

## 7. 验证与验收标准

### 核心原则
验收需覆盖“数据合规性、完整性、同步性”，由供应商先完成自查，再提交我方进行最终验收，验收通过后方可确认数据交付完成。

### 7.1 视频验收指标

#### 7.1.1 画质与音频要求（量化标准）

| 验收维度 | 合格标准 | 验证工具/方法 |
|----------|----------|---------------|
| 丢帧情况 | 单段视频丢帧率 ≤ 0.1%（即1小时视频丢帧≤3.6帧），无连续丢帧（连续丢帧≤1帧） | 1. 用 **PotPlayer** 打开视频，开启“帧计数器”（快捷键 Ctrl+J），逐段查看帧序号是否连续；<br>2. 用 **FFmpeg** 执行命令：`ffmpeg -i 视频文件.mp4 -vf "showinfo" -f null - 2> 帧信息.log`，分析日志中“frame=xxx”是否连续 |
| 画面清晰度 | 1. 游戏UI文字（如血量、技能图标）放大200%后无模糊、无锯齿；<br>2. 快速移动场景（如角色奔跑、镜头转向）无拖影、无马赛克 | 1. 用视频播放器暂停关键帧（如UI界面、快速移动场景），放大查看细节；<br>2. 对比原始游戏分辨率（如1080P）与视频分辨率，确认无拉伸/压缩（用FFmpeg查看：`ffmpeg -i 视频文件.mp4`，查看“Stream #0:0”的分辨率） |
| 音频质量 | 1. 无杂音、无爆音、无断音；<br>2. 游戏背景音、操作音效（如按键声、脚步声）音量统一（-12dB~-6dB，无忽大忽小）；<br>3. 音频与画面同步（口型/动作与音效延迟≤100ms） | 1. 用 **Audacity** 打开视频音频轨道，查看波形是否无异常尖峰（爆音）、无断层（断音）；<br>2. 用播放器播放时，重点听“操作触发音效”（如按手柄A键的音效）与画面动作是否对齐 |

#### 7.1.2 格式合规性校验
需完全符合4.1“视频交付标准”，校验项如下：

| 校验项 | 合格标准                                            | 验证工具/方法 |
|--------|-------------------------------------------------|---------------|
| 编码格式 | 视频编码：H.264（High Profile）；音频编码：AAC（LC Profile）   | 用FFmpeg查看：`ffmpeg -i 视频文件.mp4`，查看“Codec”字段（如“Video: h264”“Audio: aac”） |
| 命名规范 | 严格符合格式：`[YYYY-MM-DD] [HH-MM-SS].mp4`，无错别字、无特殊字符 | 人工核对文件名与4.1.2命名规则，重点检查“游戏名”是否与S3目录“游戏名”一致、日期/时间是否准确 |
| MD5一致性 | 视频文件的本地MD5与S3云端MD5完全一致（参考6.2.1校验报告）             | 在上传网页端“查看校验报告”，确认“校验状态”为“成功”；或在S3控制台查看文件“元数据”中的MD5值，与本地比对 |

### 7.2 键鼠手柄操作数据验收指标

#### 7.2.1 完整性要求（JSONL文件）
需完全覆盖4.2“操作数据交付标准”，无字段缺失、无事件遗漏：

| 验收维度 | 合格标准 | 验证工具/方法 |
|----------|----------|---------------|
| 元数据完整性 | 文件首行元数据包含所有必选字段（如`meta_version`、`recording_start_obs_ts`、`mouse_info`、`output_video_file`），无空值、无字段缺失 | 1. 用记事本/VS Code打开JSONL文件，查看首行；<br>2. 用 **jsonlint** 工具校验首行JSON格式（命令：`jsonlint -c 首行内容`），确认无格式错误 |
| 事件完整性 | 1. 包含所有需采集的事件类型（鼠标位移/按键/滚轮、键盘、手柄按钮/摇杆/扳机/方向键），无某类事件完全缺失；<br>2. 录制结束标识存在（文件末尾有`"type": "recording_complete"`事件） | 1. 用VS Code搜索文件内容，确认各类事件的`type`字段均存在（如搜索“mouse_move”“keyboard”“gamepad_stick”）；<br>2. 定位文件末尾，确认存在“recording_complete”事件 |
| 字段完整性 | 每个事件的`data`子字段无缺失（如鼠标按键事件必须包含`button`和`state`，手柄摇杆事件必须包含`stick`/`axis`/`value`/`normalized`） | 随机抽取10条不同类型的事件，用jsonlint校验单条JSON格式，确认无字段缺失（如抽取1条“gamepad_trigger”事件，检查是否包含`trigger`和`value`） |

#### 7.2.2 同步性要求（时间戳对齐）
确保操作数据与视频帧精准同步，无时间偏差：

| 验收维度 | 合格标准 | 验证工具/方法 |
|----------|----------|---------------|
| 时间戳有效性 | 1. 所有事件的`obs_timestamp`在元数据`recording_start_obs_ts`与`recording_end_obs_ts`区间内（误差≤10ms）；<br>2. 时间戳为整数（毫秒级），无小数、无负数 | 1. 用VS Code提取元数据中的“录制起止时间”，随机抽取20条事件的`obs_timestamp`，确认在区间内；<br>2. 检查`obs_timestamp`字段值，确认无非整数（如1727236201000.5）、无负数 |
| 视频-数据同步 | 操作事件与视频画面的时间偏差 ≤ 1ms（即事件`obs_timestamp`与对应视频帧的时间戳差值≤1ms） | 1. 选取1个明确的操作事件（如“按手柄A键”，在JSONL中找到该事件的`obs_timestamp`，记为T1）；<br>2. 用 **PotPlayer** 打开对应视频，按T1时间定位视频帧（如T1=1727236205000，对应视频时间00:00:05），确认该帧画面与“按A键”的操作动作完全对齐（如角色跳跃动作与A键事件同步） |

#### 7.2.3 数据准确性要求
确保采集的数值符合规范，无异常值：

| 验收维度 | 合格标准 | 验证工具/方法 |
|----------|----------|---------------|
| 数值范围合规 | 1. 手柄摇杆原始值（`data.value`）：-32768~32767；归一化值（`data.normalized`）：-1.0~1.0；<br>2. 手柄扳机值（`data.value`）：0~1023；<br>3. 键盘VK_CODE：0x00~0xFF | 随机抽取10条手柄摇杆/扳机事件、10条键盘事件，检查对应数值是否在合规范围内（可用Excel导入JSONL数据，筛选数值列查看） |
| 命名规范性 | 事件`type`字段（如“mouse_move”“gamepad_button”）、设备部件名称（如`data.button`的“a_button”）严格符合4.2.2.2的预设列表，无自定义名称 | 搜索JSONL文件中的`type`字段和`data.button`/`data.stick`等字段，确认无拼写错误（如“mousemove”“A_button”） |

### 7.3 验收结果判定
- **通过**：所有验收指标均满足合格标准，无一项不达标；
- **待整改**：存在1-2项非关键性不达标（如单条事件字段缺失、个别帧模糊），供应商需在24小时内整改并重新提交验收；
- **不通过**：存在关键性不达标（如MD5校验失败、大面积丢帧、时间戳偏差＞1ms），供应商需重新采集数据并提交验收。

## 8. 异常处理规范  

### 8.1 异常分级  
根据异常对数据质量、完整性及业务流程的影响程度，分为三级，具体标准如下：  

| 异常级别 | 定义标准 | 典型场景 |  
|----------|--------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------|  
| 轻微异常 | 不影响核心数据有效性，无需人工干预即可自动恢复，对最终分析结果无实质影响；异常修复后无需重新核验整体数据 | 1. 单条非关键事件字段缺失（如手柄方向键`raw_x`字段偶发缺失）<br>2. 瞬时网络波动导致的1-2帧画面轻微模糊（无连续模糊）<br>3. 单一JSONL事件时间戳偏差3-5ms（未超出≤10ms的同步阈值） |  
| 一般异常 | 部分数据受损但主体完整，需人工介入修复（无需重新采集），对分析精度有轻微影响；修复后需核验受损部分数据 | 1. 视频丢帧率0.1%~0.5%（1小时视频丢帧3.6~18帧，无连续丢帧超过3帧）<br>2. 连续5条以内同类事件缺失（如键盘按键事件短暂中断）<br>3. MD5校验单文件首次失败，重试1次后成功<br>4. JSONL文件元数据个别非关键字段缺失（如`mouse_info`中的`detected_dpi`） |  
| 严重异常 | 数据完整性或可用性遭破坏，无法通过修复恢复，需重新采集；或异常导致数据无法用于分析，需终止当前采集流程 | 1. 录制中断超过30秒（如OBS崩溃、设备断开），导致视频文件损坏无法播放<br>2. JSONL文件缺失超过20%（如关键的手柄摇杆事件大面积缺失）或核心元数据（如`recording_start_obs_ts`）丢失<br>3. 时间戳偏差＞10ms且持续存在（超过10条连续事件）<br>4. 上传失败≥3次，且排查后仍无法定位原因（如S3权限异常、网页端故障） |  


### 8.2 处理流程  

#### 8.2.1 录制中断应对措施  
1. **自动恢复机制**：  
   - 录制工具（OBS+自研程序）实时监测录制状态，若检测到中断（如OBS断开、手柄断开连接），立即触发自动重试，重试间隔10秒，最多重试3次；  
   - 若重试成功，自动生成新的片段文件，文件名序号递增（如原文件`原神_20240925_143000_01.mp4`，中断后新文件为`原神_20240925_143000_02.mp4`），避免覆盖已有有效文件；  
   - 自动记录中断日志（含中断时间、错误码如`ERR_OBS_DISCONNECT`、重试结果），存入本地`Log`目录。  

2. **人工干预流程**：  
   - 若自动重试失败，录制工具弹出告警窗口，显示中断原因及排查建议（如“检查OBS WebSocket连接”“重新插拔手柄”）；  
   - 供应商需在1小时内完成原因排查（优先检查网络、设备驱动、OBS配置），修复后重新录制缺失时段；  
   - 重新录制的文件需在文件名后加`_retry`标识（如`原神_20240925_143000_03_retry.mp4`），并在上传时备注“补录文件”；  
   - 原中断的损坏文件（如无法播放的`01.mp4`）需单独存放于S3目录`[供应商代码]/[游戏名]/broken/`下，文件名加`_broken`标识，标注中断原因（如`原神_20240925_143000_01_broken.mp4`，备注“OBS崩溃导致中断”）。  

#### 8.2.2 数据缺失应对措施  
1. **缺失检测**：  
   - 上传网页端自动校验“视频时长”与“JSONL事件时间跨度”的匹配度，允许误差≤5秒，若差异超限（如视频时长10分钟，JSONL仅记录8分钟），立即标记为“疑似缺失”并提示；  
   - 对JSONL文件执行Schema校验（预设字段规则），若检测到必选字段缺失（如`obs_timestamp`、`type`），生成《缺失报告》，明确缺失字段名称、所在行号、缺失数量。  

2. **修复策略**：  
   - 轻微缺失（单条/分散缺失）：采用“插值补全”，如手柄摇杆数据缺失时，用前后3条有效记录的均值填充；键盘事件缺失时，若前后事件无逻辑冲突，标记为“偶发缺失”无需补全；  
   - 中度缺失（≤10%且集中在某区间）：人工标注缺失时间段（如`1727236200000~1727236210000`），基于相邻有效数据的逻辑（如角色移动方向）补全，补全后在配置文件中记录修复方式（如`20240925_config.ini`备注“10:00-10:10摇杆数据插值补全”）；  
   - 严重缺失（＞10%或核心事件缺失）：触发“重新采集”流程，原缺失文件标记为`_invalid`并存档（如`原神_20240925_143000_01_invalid.jsonl`），禁止用于分析。  

#### 8.2.3 告警机制触发条件  
| 异常级别 | 触发阈值 | 通知方式 | 响应时限 | 处理要求 |  
|----------|--------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------|----------|--------------------------------------------------------------------------------------------------------------------------------------|  
| 轻微异常 | 1小时内累计出现≥5次同类轻微异常（如重复出现“单条手柄事件缺失”） | 系统日志自动记录（本地+S3日志目录），无需实时通知 | - | 每日汇总日志，排查是否存在潜在问题（如设备接触不良） |  
| 一般异常 | 1. 单批次数据中出现≥2类一般异常（如同时存在“视频丢帧”+“JSONL元数据缺失”）<br>2. 连续2个批次出现同类一般异常（如连续两批均有“MD5首次校验失败”） | 1. 上传网页端弹窗通知<br>2. 邮件通知供应商管理员（含异常报告） | 24小时 | 供应商需在时限内修复异常，修复后重新提交验收；逾期未处理则升级为严重异常 |  
| 严重异常 | 1. 检测到任何1类严重异常（如“录制中断30秒”“JSONL缺失20%”）<br>2. 一般异常逾期未处理（超过24小时）<br>3. 上传失败≥3次且原因未明 | 1. 网页端弹窗+邮件+短信通知供应商管理员<br>2. 系统工单同步我方对接人 | 2小时 | 供应商需立即停止当前采集，与我方对接人协同排查；排查完成后需提交《异常处理报告》，经我方确认后才可重启采集 |  


## 9. 术语定义  

| 术语 | 定义 | 技术说明 |  
|------|--------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------|  
| JSONL | 全称JSON Lines，一种轻量级结构化数据格式，特征为“每行一个独立JSON对象”，无嵌套数组结构 | 1. 优势：支持流式读写（无需加载完整文件即可处理单条事件），适合大规模事件日志存储；<br>2. 本规范中用途：存储键鼠手柄操作数据，每行对应1个操作事件（如鼠标移动、手柄按键），编码为UTF-8。 |  
| OBS WebSocket | OBS Studio的官方远程控制接口，基于WebSocket协议实现“客户端（自研程序）-OBS服务端”的双向通信 | 1. 核心功能：传递OBS录制状态（开始/停止）、获取视频帧时间戳、同步录制参数；<br>2. 本规范配置：默认端口4455，启用密码认证，确保自研程序与OBS的状态同步延迟≤1ms。 |  
| 归一化值 | 将原始数据按固定比例缩放至统一数值范围的标准化结果，消除不同设备硬件差异的影响 | 1. 计算逻辑：本规范中手柄摇杆原始值范围为-32768~32767，归一化值通过公式`normalized = value / 32767`计算，映射至[-1.0, 1.0]区间；<br>2. 用途：便于跨设备对比（如Xbox与PS手柄的摇杆灵敏度统一）。 |  
| S3对象键 | 唯一标识Amazon S3存储桶中“对象（文件）”的字符串，本质为“文件路径+文件名”，模拟目录结构（S3实际为扁平存储） | 1. 格式要求：UTF-8编码，最大长度1024字节，禁止包含`\`、`*`、`?`等特殊字符；<br>2. 本规范中格式：`[供应商代码]/[游戏名]/文件名`（如`SUP001/原神/原神_20240925_143000_01.mp4`）。 |  
| MD5校验 | 基于MD5哈希算法计算文件的128位哈希值，通过对比“本地文件MD5”与“云端文件MD5”验证数据完整性 | 1. 校验流程：上传前网页端自动计算本地文件MD5，上传后S3自动计算云端文件MD5，两者一致则判定“传输无损坏”；<br>2. 作用：防止文件在传输过程中因网络波动被篡改或丢失数据。 |  
| 丢帧率 | 视频录制中“丢失帧数”占“总帧数”的比例，反映视频画面的完整性 | 1. 计算公式：`丢帧率 = (总帧数 - 有效帧数) / 总帧数 × 100%`；<br>2. 本规范标准：合格丢帧率≤0.1%（即1小时60fps视频，丢失帧数≤3.6帧），无连续丢帧超过3帧。 |  
| 时间戳同步 | 确保“键鼠手柄操作事件”与“对应视频帧”的时间戳一致，避免操作与画面错位 | 1. 同步逻辑：自研程序通过OBS WebSocket获取视频帧的`obs_timestamp`，操作事件的`obs_timestamp`与该值绑定；<br>2. 本规范标准：操作事件与视频帧的时间戳偏差≤1ms，超过则影响“操作-画面”关联性分析。 |  
| Schema校验 | 基于预设的字段规则，验证JSONL文件结构是否符合规范的自动化检查机制 | 1. 校验内容：字段完整性（必选字段是否存在）、数据类型（如`obs_timestamp`是否为整数）、数值范围（如手柄扳机值是否在0~1023）；<br>2. 工具：本规范推荐使用`json-schema-validator`，校验失败则生成详细错误报告。 |  
| 供应商代码 | 我方为每个供应商分配的唯一标识，用于S3目录区分、权限控制、数据溯源 | 1. 格式：3-6位字母+数字组合（如SUP001、SUP002），由我方提前备案分配，禁止供应商自定义；<br>2. 用途：S3目录第一层、上传身份验证、日志溯源（区分不同供应商数据）。 |
